# 软著代码生成器 - 工程文件功能说明

## 概述

工程文件功能允许用户将所有配置保存到文件中，方便下次使用时快速加载，避免重复配置。工程文件采用 `.scproj` 格式（SoftCopyright Project），内部使用 JSON 格式存储。

## 功能特性

### 🔧 核心功能
- **配置保存**：将所有单个模式和批量模式的配置保存到文件
- **配置加载**：从工程文件快速恢复所有设置
- **修改跟踪**：自动检测配置变更，窗口标题显示修改状态
- **版本控制**：支持工程文件版本验证，确保兼容性
- **自动提醒**：关闭程序时提醒保存未保存的修改

### 📋 保存的配置内容

#### 单个模式配置
- 源代码路径
- 输出路径  
- 项目名称
- 文档数量
- 每个文档页数
- 是否显示文件来源信息

#### 批量模式配置
- 项目列表（支持多个项目）
- 每个项目的名称
- 每个项目的多个源代码路径
- 每个项目的文档数量和页数设置
- 批量输出目录
- 全局来源信息显示设置

#### 界面状态
- 当前选中的模式（单个/批量）
- 工程创建时间和最后修改时间

## 使用方法

### 菜单操作

#### 文件菜单选项
- **新建工程** (Ctrl+N)：清空所有配置，创建新工程
- **打开工程** (Ctrl+O)：选择并加载 .scproj 工程文件
- **保存工程** (Ctrl+S)：保存当前配置到工程文件
- **另存为** (Ctrl+Shift+S)：将当前配置保存为新的工程文件

### 操作流程

#### 1. 创建新工程
```
文件 -> 新建工程
配置各项设置（单个模式或批量模式）
文件 -> 保存工程 （选择保存位置和文件名）
```

#### 2. 打开已有工程
```
文件 -> 打开工程
选择 .scproj 文件
所有配置自动加载到界面
```

#### 3. 修改现有工程
```
打开工程文件
修改配置（窗口标题会显示 * 表示有修改）
文件 -> 保存工程 （保存修改）
```

### 修改状态指示

- **未修改状态**：窗口标题显示 "软著代码文档生成器 - 工程名.scproj"
- **已修改状态**：窗口标题显示 "软著代码文档生成器 - 工程名.scproj *"
- **新工程未保存**：窗口标题显示 "软著代码文档生成器 *"

## 工程文件格式

### 文件结构
```json
{
  "version": "1.0",
  "single_mode": {
    "source_dir": "源代码路径",
    "output_dir": "输出路径",
    "project_name": "项目名称",
    "doc_count": 文档数量,
    "pages_per_doc": 每文档页数,
    "show_source": 是否显示来源
  },
  "batch_mode": {
    "projects": [
      {
        "name": "项目名称",
        "source_paths": ["路径1", "路径2"],
        "doc_count": 文档数量,
        "pages": 页数
      }
    ],
    "output_dir": "批量输出路径",
    "show_source": 全局来源显示设置
  },
  "settings": {
    "last_mode": "single/batch",
    "created_at": "创建时间",
    "updated_at": "更新时间"
  }
}
```

### 示例工程文件

项目中包含 `example_project.scproj` 作为参考示例，展示了完整的配置结构。

## 使用建议

### 最佳实践

1. **按项目组织**：为每个软著申请项目创建独立的工程文件
2. **规范命名**：使用有意义的工程文件名，如 "智慧医疗系统_v1.0.scproj"
3. **定期保存**：配置修改后及时保存，避免丢失
4. **版本管理**：重大修改前可以使用"另存为"创建备份

### 工作流程建议

```
1. 新项目开始
   ├── 创建新工程
   ├── 配置基本信息
   └── 保存工程文件

2. 日常使用
   ├── 打开工程文件
   ├── 根据需要调整配置
   ├── 生成软著文档
   └── 保存修改

3. 项目完成
   ├── 最终确认所有配置
   ├── 生成完整的软著文档集
   └── 归档工程文件
```

## 错误处理

### 常见问题

1. **文件无法打开**
   - 检查文件是否存在
   - 确认文件格式是否正确
   - 验证 JSON 语法是否有误

2. **版本不兼容**
   - 当前版本支持 version "1.0"
   - 不同版本的工程文件可能无法打开

3. **路径不存在**
   - 加载工程文件时不会验证路径有效性
   - 生成文档时会检查并提示路径错误

### 错误恢复

- 如果工程文件损坏，可以手动编辑 JSON 文件修复
- 重要配置建议定期备份工程文件
- 程序意外关闭时，下次启动会提醒恢复未保存的修改

## 技术实现

### 关键特性
- 使用 JSON 格式确保可读性和可编辑性
- 信号阻塞机制避免加载时触发修改标记
- 自动文件扩展名处理
- 完整的错误处理和用户提示

### 兼容性
- 支持 Windows/macOS/Linux
- 兼容中文路径和文件名
- UTF-8 编码确保国际化支持

## 演示和测试

### 快速演示
```bash
python demo_project_files.py
```

### 功能测试
```bash
python test_project_file.py
```

测试覆盖：
- 配置收集和应用
- 文件保存和加载
- 修改状态跟踪
- 边界情况处理
- 错误恢复机制

## 总结

工程文件功能大大提升了软著代码生成器的易用性，让用户能够：

✅ **提高效率**：避免重复配置，一次设置长期使用  
✅ **降低错误**：减少手动输入，从文件自动加载配置  
✅ **便于管理**：不同项目使用不同工程文件，组织清晰  
✅ **支持协作**：工程文件可以分享给团队成员使用  
✅ **配置备份**：重要配置得到持久化保存  

通过工程文件功能，用户可以更加专注于软著文档的内容质量，而不必为重复的配置工作分心。 